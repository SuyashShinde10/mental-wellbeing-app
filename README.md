 
# Mental Wellbeing App
A digital sanctuary designed to help users track their emotional health and access wellness resources.

## 🛠 Tech Stack
- **Frontend:** React.js / Vite (Brutalist UI Design)
- **Backend:** Node.js / Express (Serverless on Vercel)
- **Database:** MongoDB Atlas
- **Authentication:** JWT (JSON Web Tokens)

## 🚀 Features
- **Mood Tracking:** Log daily emotional states with a minimal interface.
- **Secure Portal:** Private user accounts with encrypted data storage.

## ⚙️ Setup
1. Clone the repository.
2. Install dependencies: `npm install`.
3. Configure `.env` with `MONGO_URI` and `JWT_SECRET`.
4. Run locally: `npm run dev`.
